- config:
    name: 登录接口_正向用例
    request:
      base_url: http://localhost:8000
- test:
    extract:
    - token: content.token
    name: 登录接口_正向用例
    request:
      headers:
        Accept: application/json
        X-Lemonban-Media-Type: lemonban.v1
      json:
        password: $password
        username: $username
      method: POST
      url: /user/login/
    validate:
    - check: status_code
      comparator: equals
      expected: 200
    variables:
    - username: wangjiaqi
    - password: '123456'
